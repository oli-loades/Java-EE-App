package com.qa.service.business;

import static org.junit.Assert.*;

import org.junit.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;

import com.qa.controller.AccountController;
import com.qa.persistence.util.JSONUtility;

public class AccountValidatorTest {
	/*
	@InjectMocks
	private AccNoValidator validator;


	@Test
	public void test() {
		Mockito.when(validator.isValid("1111")).thenReturn(true);
		assertEquals(true,(validator.isValid("1111")));
		Mockito.verify(validator).isValid("1111");
		
		Mockito.when(validator.isValid("9999")).thenReturn(false);
		assertEquals(false,(validator.isValid("9999")));
		Mockito.verify(validator).isValid("9999");
	}
*/
}
